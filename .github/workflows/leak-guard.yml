name: Leak guard
on: [push, pull_request]
jobs:
  public-half:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: ./scripts/check-public-leaks.sh --all --allow-file .leak-guard-allow
