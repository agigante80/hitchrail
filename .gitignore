__pycache__/
*.py[cod]
.venv/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.import_linter_cache/
docs/design/hitchrail-session-manager.html
.claude/settings.local.json
CLAUDE.local.md
.coverage
.claude/overnight/

# Never published. It holds the project rules, agents and skills that name
# this machine and its projects, and a public repository is the wrong place
# for any of that. Kept locally, deliberately.
.claude/

# mutmut copies the tree here, one file per module holding every mutant of it.
# Regenerated by `uv run mutmut run`, never edited, and large.
mutants/
.mutmut-cache
